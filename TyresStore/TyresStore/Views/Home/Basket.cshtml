@model IList<TyresStore.Repository.Models.Basket>

@if (Model.Count > 0)
{
    <div class="table" style="width: 100%">
        <table class="table">
            <thead>
                <tr>
                    <th>TyreId</th>
                    <th>Description</th>
                    <th>Date added</th>
                </tr>
            </thead>
            <tbody>
                @foreach (var item in Model)
                {
                    <tr>
                        <td>@item.TyreId</td>
                        <td>@item.Description</td>
                        <td>@item.AddedDate</td>
                        <td class="delete-item">
                            <button type="button" class="btn btn-default btn-md" onclick="basketModel.deleteItem(@item.ID)">
                                Delete Item
                                <span class="glyphicon glyphicon-trash"></span>
                            </button>
                        </td>
                    </tr>
                }
            </tbody>
        </table>
    </div>
}

